arr = [15, 7, 18, 5, 12, 8, 5, 1]

arr.index(5) 
# this returns the index of the first occurance of 5, which is index 3
arr.index[5]
# this returns a syntax error for square brackets being used for a method call instead of parentheses
arr[5]
# this returns the element with index 5, which is 8